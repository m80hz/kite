[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kite"
version = "0.1.0"
description = "Research code for KITE"

[tool.setuptools]
package-dir = {"" = "."}

